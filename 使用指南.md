# 使用指南（新手向，Windows）

> 跟着做就行，每一步都给了可以直接复制的命令。
> 所有命令都在「命令提示符 / PowerShell」里输入，目录都是 `D:\Project1\math_rag`。

## 0. 前提：装好 Python

在开始菜单搜索「命令提示符」，打开后输入：

```cmd
python --version
```

- 显示 `Python 3.10.x` 或更高 → 继续下一步。
- 显示"不是内部或外部命令" → 没装或没加 PATH。
  去 https://www.python.org/downloads/ 下载安装，安装时**一定要勾选 "Add Python to PATH"**。

## 1. 进入项目目录 + 创建虚拟环境（一次性）

```cmd
cd /d D:\Project1\math_rag
python -m venv .venv
.venv\Scripts\activate
```

成功后命令提示符最前面会出现 `(.venv)`。
以后每次重新打开终端，只需要：
```cmd
cd /d D:\Project1\math_rag
.venv\Scripts\activate
```

## 2. 安装依赖（一次性，国内建议用清华镜像加速）

```cmd
pip install -e ".[dev]" -i https://pypi.tuna.tsinghua.edu.cn/simple
```

这一步要下载 torch 等库，比较大，耐心等。装完再执行：

```cmd
set HF_ENDPOINT=https://hf-mirror.com
```

> 这行是让 HuggingFace 模型从国内镜像 hf-mirror.com 下载
> （默认小模型约 420MB；如换成 bge-m3 则约 2GB），
> 只在当前这个终端窗口有效，**下载模型的那次运行前都要设一下**。
> 若你的网络能直连 huggingface.co（报错 `FileMetadataError` 就说明镜像不兼容），
> 执行 `set HF_ENDPOINT=` 清空后直连官方源即可。

## 3. 第一步：先检查你的 50 篇论文能不能被读进来（不下载模型，很快）

```cmd
python scripts\run_experiment.py --dry-run
```

- 会列出每篇论文的文件名、字符数、预计切多少块；
- 如果哪篇没被加载，日志里会说明原因（比如 `tchi-2.pdf_tex` 是插图导出件，没有 `\documentclass`，属正常跳过）；
- 如果有论文引用 `\input{xxx}` 但子文件没拷进来，会出现 WARNING，不影响主流程。

## 4. 第二步：小规模试跑（只用 5 篇，确认整条链路能跑通）

```cmd
set HF_ENDPOINT=https://hf-mirror.com
python scripts\run_experiment.py --limit 5 --eval
```

首次运行会先下载嵌入模型（默认小模型约 420MB），之后就快了。
成功后生成 `logs\experiment.json`，里面是两组（baseline / math_aware）的检索结果和评估指标。

## 5. 第三步：全量运行（50 篇）

```cmd
python scripts\run_experiment.py --eval
```

> ⚠️ 50 篇论文 + CPU，默认小模型也要跑几十分钟到一两个小时，建议睡前挂着跑。
> 默认的 `paraphrase-multilingual-MiniLM-L12-v2` 是多语言小模型（快、够用）；
> 想追求更好的检索质量，把 `config.yaml` 里 `model_name` 换成 `BAAI/bge-m3`
> （多语言大模型，约 2GB，CPU 上慢 4~5 倍，建议晚上或换 GPU 跑）。

## 6. 第四步：交互式问答 Agent

```cmd
python scripts\run_experiment.py --agent
```

- **react 模式（默认）**：Agent 自己决定调哪些工具，屏幕上会显示它每一步在干什么
  （比如 `[第 1 步] 调用工具 search_papers ...`），最后给出带证据标注的答案；
- **simple 模式（旧流水线）**：检索一次 → 显示证据 → DeepSeek 回答，适合和 react 做对比：

```cmd
python scripts\run_experiment.py --agent --agent-mode simple
```

输入英文或中文问题（如 "What is the first eigenvalue of the p-Laplacian under Neumann conditions?"）。

想用 DeepSeek 回答，先设置密钥（到 https://platform.deepseek.com 申请）：

```cmd
set DEEPSEEK_API_KEY=sk-你的密钥
python scripts\run_experiment.py --agent
```

## 7. 跑单元测试（确认代码本身没问题）

```cmd
pytest
```

看到 21 个 passed 就 OK。

## 8. 看懂结果文件 logs\experiment.json

- `baseline_retrieve` / `math_aware_retrieve`：两组各自检索到的片段（含 `source` 论文名、`chunk_id`）；
- 末尾 `evaluation` 块（带 `--eval` 才有）：
  - `mean_recall`：相关论文被捞回来的比例（越高越好）；
  - `mean_mrr`：第一个相关结果排第几（1.0 表示每次都第一名命中）；
  - `mean_ndcg`：整体排序质量（1.0 最好）；
- 对比两组数值，就能说"哪种切分方案更好"。

## 9. 想评估你自己的 50 篇论文（重要）

项目自带的 `data\eval\ground_truth.json` 只是演示（标注的是两个示例论文）。
要得到有说服力的实验结论，需要你自己标一份：

1. 新建 `data\eval\ground_truth_mine.json`，格式照抄：
```json
[
  {"question": "What is the p-Laplace operator?", "relevant": ["p_laplacian_v01"]},
  {"question": "How is the first eigenvalue characterized?", "relevant": ["eigenvalue3", "sobeig"]}
]
```
其中 `relevant` 填的是**论文文件名（去掉 .tex）**，即"哪个论文里能找到这个问题的答案"。
2. 改 `config.yaml` 里 `ground_truth: data/eval/ground_truth_mine.json`。
3. 重新跑 `python scripts\run_experiment.py --eval`。

标注 10~20 个问题就够面试用了。

## 10. 常见问题

| 现象 | 原因 / 解决 |
|---|---|
| 模型下载卡住 | 网络问题，重试；走镜像先执行 `set HF_ENDPOINT=https://hf-mirror.com` |
| 模型下载报 FileMetadataError | 镜像与新版本 huggingface_hub 不兼容：执行 `set HF_ENDPOINT=` 清空后直连官方源 |
| 提示 `ModuleNotFoundError` | 没激活虚拟环境，先执行 `.venv\Scripts\activate` |
| 跑得特别慢 | 换小模型（见第 5 步），或用 `--limit 5` 试跑 |
| 某篇论文没加载 | `--dry-run` 看日志：无 `\documentclass` 的会跳过；缺 `\input` 子文件只告警不报错 |
| 中文问题检索差 | 确认 config.yaml 用的是多语言模型（`paraphrase-multilingual-MiniLM-L12-v2` 或 `BAAI/bge-m3`），不是 `bge-small-en-v1.5`（纯英文） |
