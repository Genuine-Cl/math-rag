# 实验结果（面试用）

> 实验日期：2026-06（机器：Windows，CPU，Python 3.11）
> 复现命令：`python scripts\run_experiment.py --eval`，结果自动写入 `logs\experiment.json`

## 一、实验设置

| 项目 | 设置 |
|---|---|
| 语料 | 45 篇 p-Laplacian / 特征值 / 谱几何方向 LaTeX 论文，清洗后约 360 万字符 |
| 对比对象 | baseline（通用递归切分） vs math_aware（公式环境原子化切分 + block_type 标记） |
| 嵌入模型 | paraphrase-multilingual-MiniLM-L12-v2（多语言，CPU 推理） |
| 检索策略 | 两组完全一致：MMR（k=3, fetch_k=12, λ=0.7）+ BM25 混合检索（RRF 融合，稠密权重 0.6） |
| 评估 | 15 个逐篇核对的问题（论文级标注），指标 Recall@3 / MRR / nDCG@3 |

## 二、结果

| 指标 | baseline | math_aware | 差异 |
|---|---|---|---|
| Recall@3 | 0.8556 | 0.8556 | 持平 |
| MRR | 0.8667 | **0.9000** | +3.3pp |
| nDCG@3 | 0.8290 | **0.8536** | +2.5pp |

**结论**：数学感知切分在排序质量上稳定小胜（MRR +3.3pp、nDCG +2.5pp），召回持平。
两组共用同一嵌入模型、同一检索参数、同一评估集，保证对照公平。

## 三、逐题亮点

- 15 题中 12 题两组均将相关论文排入 top-3；
- 典型案例：问"Pólya 猜想"时，baseline 将相关论文排第 2（MRR 0.5），
  math_aware 排第 1（MRR 1.0）——公式原子化保留了该论文结论语句与公式的完整上下文；
- 已知缺陷 1：定义类问题（如"What is the p-Laplace operator"）两组均失败，
  检索返回相邻主题论文（对数 p-Laplacian，属"近义命中"），真正给出定义的两篇未进 top-3；
- 已知缺陷 2：部分问题只召回多篇相关论文中的 1~2 篇（Recall < 1）。

## 四、过程中的两个工程发现（面试亮点）

1. **nDCG 出现 >1 非法值**：定位到"论文级标注 vs chunk 级检索"的粒度错配
   ——同一论文多个 chunk 同时进 top-k 导致重复计分。修复为每篇论文仅第一次命中计分，
   并补回归测试（tests/test_evaluation.py::test_ndcg_counts_each_paper_only_once）。
2. **第一轮评估 3 题 0 分并非检索错误**：用 `retrieved_sources` 溯源发现是标注不完整
   （检索到的文档确实相关但未标注）。补全标注后复评，Recall 由 0.8 升到 0.856，
   且揭示了"检索系统其实比指标显示的更好"这一事实。

## 五、Agent 对比实验（RAG → Agent 升级）

> 复现命令：`python scripts\run_experiment.py --agent`（react 模式）、
> `python scripts\run_experiment.py --agent --agent-mode simple`（固定流水线）。
> 两种模式共用同一语料、同一向量库、同一混合检索器和同一 DeepSeek 模型，对比公平。

### 5.1 两种模式的定义

| | simple（RAG） | react（Agent） |
|---|---|---|
| 检索次数 | 固定 1 次，取 top-k | 不限（受 `max_iterations` 护栏约束） |
| 决策权 | 代码写死，LLM 无决策权 | LLM 每轮自主决定"调哪个工具、用什么参数" |
| 工具 | 无 | search_papers / get_chunk / list_papers（function calling） |
| 首轮召回失败时 | 只能如实拒答 | 可改写 query 重搜、深挖 chunk，自我修正 |
| 单次问答 LLM 调用 | 1 次 | 约 4~6 次 |

### 5.2 实验一：语料内问题（首轮召回恰好失败）

问题："论文库里哪些文章讨论了 p-Laplacian 的第一特征值？它们的主要结论是什么？"

| | simple | react |
|---|---|---|
| 行为 | 检索 1 次，top-3 恰好是"最大值原理"chunk 和两段标题性文本，均不含结论 | 5 轮工具调用：先 list_papers 摸清语料，检索后自动改写 query（`lower bound estimate` 等）重搜，get_chunk 深挖关键 chunk |
| 结果 | "当前文献库没有足够证据回答该问题"（诚实拒答） | 三篇主文献 + 一篇补充的完整综述，含 LaTeX 公式与逐条证据标注 |
| 成本 | 1 次 LLM 调用 | 6 次 LLM 调用 |

**关键机制**：检索器每次只回 top-3，而不同 query 的 top-3 窗口是不同的。
simple 押注在单次检索的窗口上；react 通过改词重搜把窗口的多个切片拼起来，
召回就补齐了。这正是 Agent 相对 RAG 的核心价值：**首轮召回失败时能自我修正**。

### 5.3 实验二：语料外问题（验证诚实性）

问题："论文库里有没有讨论 p-Laplacian 在图像去噪上的应用？"（语料中没有直接答案）

- 行为：4 次检索（英文→扩展关键词→中文，多角度改写）探明语料边界；
- 结果：明确回答"没有专门讨论图像去噪的论文"，同时找到最接近的 `main (3)`
  （hypergraph p-Laplacian 图像修复 inpainting），并主动区分
  "inpainting（修复）≠ denoising（去噪）"，把不相关论文逐一归类为"相关但不涉及去噪"；
- 观察点：第 3 步中文 query 召回明显衰减（捞回 "Background" 标题性片段），
  模型随后自动切回英文 query —— 印证了选多语言嵌入模型 + 混合检索的既有设计决策；
- **没有为了讨好问题而编造任何证据** —— 证据约束在 Agent 模式下依然生效。

### 5.4 开发过程中的两个工程问题（面试亮点）

1. **初版 Agent 不收尾**：模型在相关话题上无限发散检索，跑满 6 轮被强制停止、
   一个答案都没有。修复：①系统提示词加硬规则（同一问题检索 2~3 轮就该综合作答）；
   ②预算快用完时注入"必须收尾"提醒；③兜底——超预算仍不收敛时，把已收集证据汇总、
   用**不绑定工具的裸模型**强制生成答案。三层护栏保证每个问题一定有回答。
2. **未设 API Key 却先建向量库**：密钥检查原来在建库之后，没设 key 也会先埋头
   嵌入几万个 chunk（CPU 上十几分钟，且无进度输出，看起来像死循环）。
   修复：react 模式启动时前置检查密钥、秒退；建库前后加进度日志，区分"在干活"与"真卡住"。

### 5.5 已知缺陷与后续

- **引用忠实度**：react 的综述答案中观察到公式转述失真——变分刻画公式里
  分子是 `v`、分母却写成了 `|u|^p`（原文应为同一函数）。这是 RAG 经典问题：
  模型"引用了"证据，复述时却悄悄改符号。后续方向：LLM-as-judge 用检索原文
  核对公式，或对公式类内容要求逐字引用；
- 量化评估：给 Agent 建评估集，统计成功率 / 平均工具调用轮数 / 单问成本，
  把"Agent 比 RAG 好"从行为演示变成数据。

## 六、改进方向（未来工作）

- 定义类问题：query 改写 / 同义词扩展（react 模式已演示模型自动改写检索词，
  下一步是在评估集上量化增益）；
- 消融实验：k、λ、混合权重、chunk_size 各取几档对比；
- 引入 cross-encoder 重排，或换 bge-m3 提升向量质量；
- 加 LLM-as-judge 评估生成答案的正确性与引用忠实度。

## 七、两分钟面试自述（模板）

> 我做了一个面向 LaTeX 数学论文的检索增强问答系统。文献加载部分递归展开 \input、
> 清洗导言区和参考文献，并把论文名写入元数据保证结果可溯源。切分部分我对比了
> 两种策略：通用递归切分，和"数学感知"切分——后者先把公式环境整体替换成占位符，
> 切分后再还原，保证公式不会被拦腰切断，并给每个块打上公式/文本标签。
> 检索用 MMR 加 BM25 的混合方案，RRF 融合是我自己实现的，避免依赖版本漂移。
> 为了证明效果，我建了 15 个问题、逐篇核对过的评估集，算 Recall@3、MRR、nDCG@3：
> 数学感知组在 MRR 上高 3.3 个百分点、nDCG 高 2.5 个百分点，召回持平。
> 评估过程中我修过一个 nDCG 超 1 的指标 bug——根因是论文级标注和 chunk 级检索的粒度错配，
> 还通过溯源发现三个零分问题其实是标注不完整而不是检索失败。
> 最后我把系统从 RAG 升级成了 Agent：用 function calling 给模型三个工具
> （检索、按 chunk 取原文、列出语料），让它自主决定检索策略。对比实验里，
> 同一个问题固定流水线一次检索没中关键片段、只能诚实拒答，Agent 通过改写 query
> 重搜补全了召回，给出三篇文献的完整综述；问语料外的问题时它也能诚实划界、不编造。
> 代价是单次问答的 LLM 调用从 1 次升到约 4~6 次，所以两种模式都保留、按场景切换。
> 后续计划是定义类问题的 query 改写量化、超参消融、重排模型，以及给 Agent
> 建评估集并补引用忠实度校验。
